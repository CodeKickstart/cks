
import { Str } from "../../../defs/types/Str";


export const opsClient = () => {
  const fnPreProcess = (

  ): {
    error: Str;
  } => {

    return { error: null };
  };

  const fnPostProcess = (

  ): {
    error: Str;
  } => {
    return { error: null };
  };

  return { fnPreProcess, fnPostProcess };
};
